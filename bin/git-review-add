#!/bin/bash
set -eu

remote="origin"
branch=${1?"specify target branch"}
basename="$(basename $(pwd))"
path="../$basename-review/$branch"

git worktree add "$path" -b "$branch" "$remote/$branch"
cd "$path"
git review dev
open -a xcode .
